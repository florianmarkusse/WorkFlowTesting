on: [pull_request, push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: Run jsPerformoBot
      uses: florianmarkusse/jsPerformoBot@
      with:
        repo-token: ${{secrets.GITHUB_TOKEN}}"